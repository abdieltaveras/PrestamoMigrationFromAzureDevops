@model IEnumerable<PrestamosMVC5.SiteUtils.Element>
@using System.Linq
@using PrestamosMVC5.SiteUtils


    @*@helper DisplayTree(IEnumerable<Element> elements)
    {
        <ul>
            @foreach (var element in elements)
            {
                <li>
                    @element.Nombre
                    @{
                        var children = Model.Where(e => e.Padre == element).OrderBy(e => e.Id);
                        if (children.Count() > 0)
                        {
                            @DisplayTree(children)
                        }
                    }
                </li>
            }
        </ul>
    }*@

    @*@DisplayTree(Model.Where(e => e.Padre == null).OrderBy(e => e.Id))*@
