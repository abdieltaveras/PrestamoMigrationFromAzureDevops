@model IEnumerable<Garantia>
<link href="~/Content/customcss/datatable/css/customStyle.css" rel="stylesheet">

<div class="row">
    <div id="listContainer" class="col-md-12 col-sm-12 col-xs-12">

        <h4>Tabla de Garantias</h4>
        <div class="x_panel">

            <div class="x_content">
                @* data *@
                <div class="table-responsive">
                    <table id="datatable" class="table-bordered row-border">
                        <thead>
                            <tr>
                                <th class="checkColumn">
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IdGarantia)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IdModelo)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IdNegocio)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IdTipoGarantia)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.IdClasificacion)
                                </th>
                                

                                @*<th></th>*@
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                <tr class=" " data-id="@item.IdGarantia">
                                    <td>
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IdGarantia)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IdModelo)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IdNegocio)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IdTipoGarantia)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.IdClasificacion)
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @* table *@

            </div>
        </div>
    </div>
</div>
<script>
    let onEditButton = function () {
        let id = $('tr.selected').data("id");

        if (id) {
            window.location.href ='/Garantias/CreateOrEdit/' + id;
        }
    }
    //let onDeactivateButton = function () {
    //    let id = $('tr.selected').data("id");

    //    if (id) {
    //        window.location.href ='/Clientes/Anular/' + id;
    //    }
    //}
    //let onCancelButton = function () {
    //    console.log('Delete');
    //}
    let onNewButton = function () {
        window.location.href ='/Garantias/CreateOrEdit';
    }

    $(document).ready(function () {
        // Datatable
        var table = $('#datatable').DataTable(dataTableConfiguration);
        removeTemplateConfiguration();
    });
</script>