@page "/marcas"
@inherits BaseForCreateOrEdit

<CreateOrEditCatalogo Catalogo="Catalogo" CatalogoGetParams="CatalogoGetParams"></CreateOrEditCatalogo>

@*<h1>Marcas  <button class="btn btn-primary" @onclick="()=>CreateOrEdit()">Agregar</button></h1>*@



@*<button class="btn btn-info" @onclick="GetMarcas">Buscar</button>*@

@*<button class="btn btn-primary" @onclick="GetMarcasByParam">Get By Param</button>
    <button class="btn btn-primary" @onclick="GetAll">Get All</button>
    <button class="btn btn-primary" @onclick="Clear">Limpiar lista</button>*@


@*<div class="modal fade" id="edtMarca" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                @if (Marca.IdMarca > 0)
                {
                    <h5 class="modal-title" id="edtMarcaTitel">Editar Marca</h5>
                }
                else
                {
                    <h5 class="modal-title" id="edtMarcaTitel">Crear Marca</h5>
                }

                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="col-md-12">
                <EditForm Model="@Marca" OnValidSubmit="SaveMarca" OnInvalidSubmit="RaiseInvalidSubmit">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label for="codigo">Codigo</label>
                                <InputText id="codigo" class="form-control col-md-4" @bind-Value="Marca.Codigo"></InputText>
                            </div>

                            <div class="form-group">
                                <label for="nombre">Nombre</label>
                                <InputText id="nombre" class="form-control col-md-6" @bind-Value="Marca.Nombre"></InputText>
                            </div>
                            <div class="modal-footer">
                                <button type="submit" class="btn btn-primary" >Guardar</button>
                            </div>
                        </div>
                    </div>
                </EditForm>
            </div>

        </div>
    </div>
</div>*@





<!--<div class="row">
    <div class="col-md-12">
        <div class="search float-right">
            <EditForm autocomplete="off" Model="@SearchMarca" OnValidSubmit="@GetMarcas">
                <DataAnnotationsValidator />
                <ValidationSummary />-->
                <!--<div class="input-group">-->
                <!--<InputText id="nombres" @bind-Value="SearchMarca.IdMarca" placeholder="Nombre" class="form-control" />
                <span>
                        <button type="submit" class="btn btn-info">Buscar</button>
                    </span>
                </div>

            </EditForm>
        </div>
    </div>
</div>-->


@*<table class="table table-responsive-md table-hover">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Operacion</th>
        </tr>
    </thead>
    <tbody>
        @if (marcas.Count() > 0)
        {
            @foreach (var item in marcas)
            {
                <tr>
                    <td>@item.Nombre</td>
                    <td> <button @onclick="()=>CreateOrEdit(item.IdMarca)" class="btn btn-info"> Editar </button></td>
                </tr>

            }
        }
        else
        {
            <p> no existen datos</p>

        }
    </tbody>
</table>*@