@inherits BaseForCreateOrEdit
<MudDialog>
    <DialogContent>
        <MudGrid>
            <MudItem md="4" lg="4" xs="12" sm="12">
                <MudSelect T="int" @bind-Value="SelectedPropertySearch" Label="Buscar Por...">
                    @foreach (var item in EnumToList.TipoBusquedaPrestamo())
                    {
                        <MudSelectItem Value="@item.Value">@item.Text</MudSelectItem>
                    }
                </MudSelect>
            </MudItem>
            <MudItem md="7" lg="7" xs="12" sm="12">
                @*<MudNumericField T="int" @bind-Value="GetParams.IdPrestamo" Label="Buscar..."></MudNumericField>*@
                <MudTextField @bind-Value="SearchText" Label="Buscar..."></MudTextField>


                <MudItem md="1" lg="1" xs="12" sm="12">
                    <MudButton OnClick="onSearchClick">Buscar</MudButton>
                </MudItem>
            </MudItem>
      
        </MudGrid>
        <MudGrid>
            <MudItem md="12" lg="12" xs="12">
                @if (prestamos.Count() > 0)
                {
                    <MudTable Items="@prestamos" Hover="true" Breakpoint="Breakpoint.Sm">
                        <HeaderContent>
                            <MudTh>IdPrestamo</MudTh>
                            <MudTh>Identificacion</MudTh>
                            <MudTh>Nombre</MudTh>

                            <MudTh>Seleccionar</MudTh>
                        </HeaderContent>
                        <RowTemplate>
                            <MudTd DataLabel="IdPrestamo">@context.IdPrestamo</MudTd>
                            <MudTd DataLabel="Identificacion">@context.NumeracionDocumentoIdentidad</MudTd>
                            <MudTd DataLabel="Nombre Completo">@context.NombreCompleto</MudTd>
                            <MudTd>
                                <MudIconButton Icon="@Icons.Filled.Add" OnClick="()=>SelectedValue(context)" />
                            </MudTd>
                        </RowTemplate>
                    </MudTable>
                }

            </MudItem>
        </MudGrid>

    </DialogContent>
    <DialogActions>

    </DialogActions>
</MudDialog>

@code {

}