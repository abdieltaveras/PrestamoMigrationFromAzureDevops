@inherits BaseForSearch;
<div class="row">
    <div class="col-sm-12">
        <EditForm Model="@Localidades">
            <DataAnnotationsValidator />
            <ValidationSummary />
            <div class="form-group">
                <div class="input-group mb-3">
                    <input class="form-control" @bind-value=@buscarLocalidad.Search placeholder="Localidad A Buscar" minlength="2" />
                    <div class="input-group-append">
                        <button class="btn btn-outline-secondary btn-sm" type="button" @onclick=@GetLocalidadesAll><i class="fa fa-search"></i></button>
                    </div>
                </div>
            </div>

            @*<div id="list-address-container">
                <div class="list-group col-md-12" id="list-tab" role="tablist" style="position: absolute; z-index:2; padding: 0%; vertical-align: middle;">
                    @if (Localidades.Count() > 0)
                    {
                        @foreach (var item in Localidades)
                        {
                            <p class="list-group-item direcciones list-group-item-action" id="listaddresses" data-toggle="list"
                               data-idlocalidadpadre="@(item.IdTipoLocalidad)"
                               data-idlocalidad="@(item.IdLocalidad)" href="#list-home" role="tab" aria-controls="home">
                                <strong id="placeName">@(item.Nombre)</strong>
                                <span style="float: right;">
                                    @(item.Descripcion)
                                    <button type="button" class="btn btn-outline-info"> <i class="fa fa-edit"  @onclick=@GetLocalidadesAll2(item.IdLocalidad)></i> </button>
                                </span>
                            </p>
                            }
                        }
                    </div>
            </div>*@
            <div class="form-group">
                @if (Localidades.Count() > 0)
                {
                    
                 <select @bind="SelectedLocalidad" class="form-control">
                     <option value="">Seleccione una Localidad</option>
                    @foreach (var item in Localidades)
                    {
                        <option value=@item.IdLocalidad>@(item.Nombre) ||Tipo:  @(item.Descripcion) </option>
                    }
                </select>
                }
            </div>

        </EditForm>
    </div>
</div>


@code {

}
